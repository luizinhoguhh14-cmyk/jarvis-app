allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = '../build'

subprojects {
    tasks.configureEach { task ->
        if (task.name.contains('Metadata')) {
            enabled = false
        }
    }
}
