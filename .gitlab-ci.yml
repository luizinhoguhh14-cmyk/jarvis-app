image: ghcr.io/cirrusci/flutter:3.24.0

stages:
  - build

build_apk:
  stage: build
  script:
    - flutter pub get
    - flutter build apk --debug
  artifacts:
    paths:
      - build/app/outputs/flutter-apk/app-debug.apk
    expire_in: 1 week
